"""Shared utilities (data loading, benchmark helpers, etc.).

Modules:
  • `data_utils`   — `OnlineDataset` image+mask loader with augmentation
  • `sav_utils`    — SAV / VOS inference helpers
  • `sav_benchmark` — SAV benchmark glue
  • `utils`        — misc helpers
  • `visualize_benchmark_results` — CSV → plot helpers
"""
